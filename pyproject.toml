[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "recall-monitor"
version = "0.1.0"
description = "Incremental SAMR recall review and Jiandaoyun synchronization"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
recall = "recall.cli:main"

[tool.setuptools]
packages = ["recall"]
